<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Style TargetType="Label" x:Key="defaultLabel">
        <Setter Property="Foreground" Value="#FFF7F4F3" />
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="20"/>
    </Style>
    
    <Style TargetType="Label" BasedOn="{StaticResource defaultLabel}" x:Key = "heading">
        <Setter Property="FontSize" Value="28" />
        <Setter Property="BorderThickness" Value="0,0,0,3" />
        <Setter Property="Padding" Value="15,5,15,5" />
        <Setter Property="BorderBrush">
            <Setter.Value>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <LinearGradientBrush.RelativeTransform>
                        <TransformGroup>
                            <ScaleTransform CenterY="0.5" CenterX="0.5" />
                            <SkewTransform CenterX="0.5" CenterY="0.5" />
                            <RotateTransform Angle="90" CenterX="0.5" CenterY="0.5" />
                            <TranslateTransform />
                        </TransformGroup>
                    </LinearGradientBrush.RelativeTransform>
                    <GradientStop Color="#FFBA1B1D" />
                    <GradientStop Color="#FF5B2333" Offset="1" />
                    <GradientStop Color="#FF564D4A" Offset="0.5" />
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="TextBlock" x:Key="errorTextBlock">
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="Foreground" Value="#FFBA1B1D"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style> 

    <Style TargetType="ListBox" x:Key="defaultListBox">
        <Setter Property="Foreground" Value="#FFF7F4F3" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Background" Value="#FF2D2D2D"/>
        <Setter Property="ScrollViewer.Visibility" Value="Visible"/>
    </Style>

    <Style TargetType="Button" x:Key="defaultButton">
        <Setter Property="Padding" Value="10,5"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Background" Value="#202020"/>
        <Setter Property="Foreground" Value="#FFF7F4F3"/>
        <Setter Property="BorderBrush" Value="#FFF7F4F3"/>
    </Style>

    <Style TargetType="TextBox" x:Key="defaultTextbox">
        <Setter Property="Background" Value="#202020"/>
        <Setter Property="Foreground" Value="#FFF7F4F3"/>
        <Setter Property="FontSize" Value="15"/>
    </Style>

</ResourceDictionary>